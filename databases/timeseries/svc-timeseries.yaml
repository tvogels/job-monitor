# InfluxDB timeseries database --
# This 'service' makes sure the open ports in the corresponding 'pod' are accessible.
# usage: kubectl create -f svc-timeseries.yaml
apiVersion: v1
kind: Service
metadata:
  name: vogels-timeseries
  labels:
    name: vogels-timeseries
    user: vogels
spec:
  type: NodePort
  ports:
  - name: http
    port: 8086
    nodePort: 30086
    protocol: TCP
  selector:
    name: vogels-timeseries
